# =============================================================================
# FRONTEND ENVIRONMENT CONFIGURATION
# Copy this file to .env and update the values for your deployment
# =============================================================================

# =============================================================================
# HOST CONFIGURATION - MUST MATCH BACKEND CONFIGURATION
# =============================================================================
VITE_HOST_IP=localhost
VITE_BACKEND_PORT=8000
VITE_BACKEND_URL=http://localhost:8000

# =============================================================================
# EXAMPLE CONFIGURATIONS
# =============================================================================

# Example 1: Local Network Deployment (192.168.1.100)
# VITE_HOST_IP=192.168.1.100
# VITE_BACKEND_PORT=8000
# VITE_BACKEND_URL=http://192.168.1.100:8000

# Example 2: Production Server with Domain
# VITE_HOST_IP=your-domain.com
# VITE_BACKEND_PORT=8000
# VITE_BACKEND_URL=http://your-domain.com:8000

# =============================================================================
# NOTES
# =============================================================================
# - All VITE_ prefixed variables are available in the frontend code
# - VITE_HOST_IP should match the HOST_IP in backend .env
# - VITE_BACKEND_URL should be the complete URL to your backend server
# - These variables are embedded at build time, so restart dev server after changes 